server:
  port: 9081

spring:
  #  spring:
  #    profiles:
  #      active: dev
  application:
    name: claim-management

  datasource:
    url: jdbc:sqlserver://serverbcprogrammers.database.windows.net:1433;database=demo;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;
    username: adminbcp
    password: BCProgrammers1.
    initialization-mode: always